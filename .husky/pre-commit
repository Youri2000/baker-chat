# 提交前：只对暂存文件运行 lint 与格式化（配置在根 package.json 的 lint-staged 字段）
# 💡 只检查暂存文件（按 glob 分派 eslint / prettier / ruff），全量检查交给 CI；提交信息由 commit-msg 的 commitlint 校验，详见 docs/interview.md#git-hooks
pnpm exec lint-staged
